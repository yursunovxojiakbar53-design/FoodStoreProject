spring.application.name=FoodStore
spring.datasource.url=jdbc:postgresql://localhost:5432/foodstore
# Use environment variables for credentials in production. Set DB_USERNAME and DB_PASSWORD in your environment.
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:123qwe&&}
spring.jpa.hibernate.ddl-auto=update
server.port=8081
file.upload-dir=uploads
spring.jackson.deserialization.fail-on-null-for-primitives=false
# Email (Gmail example) - don't store real passwords here. Set MAIL_USERNAME and MAIL_PASSWORD in env.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:yursunovxojiakbar53@gmail.com}
spring.mail.password=${MAIL_PASSWORD:glmeopptrpimmtcq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Telegram Bot (set TELEGRAM_BOT_TOKEN and TELEGRAM_BOT_USERNAME in env)
telegram.bot.token=${TELEGRAM_BOT_TOKEN:your-bot-token}
telegram.bot.username=${TELEGRAM_BOT_USERNAME:FoodStoreBot}
telegram.bot.enabled=${TELEGRAM_BOT_ENABLED:true}
telegram.bot.admin-chat-ids=${TELEGRAM_ADMIN_CHAT_IDS:}
telegram.bot.page-size=5
telegram.bot.backend-base-url=http://localhost:8081
